# Page Spec — {Page Name}

> Copy this file into `docs/` as `{route-slug}.md` (for example
> `docs/ride-trip-details.md`) after analyzing the reference image. Fill every
> section from the reference. Leave a section as "Not observed" rather than
> guessing.

## Page name

{Page Name}

## Route

/{route-slug}

## Reference image

{file name or path}

## Reference viewport

{width × height of the reference image}

## Overall composition

Describe the full-page composition: what sits where, in what order, and how the
page is divided.

## Desktop layout

Describe the desktop structure: header, hero, sections, footer, alignment,
columns.

## Mobile layout

Describe how the reference behaves at mobile width: what stacks, what collapses,
what is hidden, what changes order. Do not assume the desktop layout is shrunk.

## Container width

{max content width, and how it is centered}

## Section structure

Ordered list of sections with their purpose and boundaries.

## Typography

| Element | Font family | Size | Weight | Line height | Letter spacing | Color |
| --- | --- | --- | --- | --- | --- | --- |
| | | | | | | |

## Font weights

List every weight actually used.

## Colors

List every observed color with its hex/RGB value and where it is used
(backgrounds, text, accents, borders).

## Spacing

Record page padding, section gaps, component padding, and gaps between elements.

## Component dimensions

| Component | Width | Height | Notes |
| --- | --- | --- | --- |
| | | | |

## Cards

Structure, padding, radius, border, shadow, internal layout of each card.

## Buttons

Label, size, padding, radius, fill, border, text color, icon, position.

## Navigation

Items, order, style, position, behavior at mobile width.

## Icons

Which icons appear, their library/style, and their sizes.

## Imagery/assets

Every image, illustration, avatar, logo, or map: subject, position, dimensions,
aspect ratio, cropping.

## Borders

Color, width, style, radius per element.

## Shadows

Color, offset, blur, spread per element.

## Gradients

Type, direction, color stops, opacity.

## Responsive behavior

Breakpoints observed or implied, and exactly what changes at each.

## Animation observations

What moves, on what trigger, duration, easing, direction, and whether it repeats.

## Implementation notes

Technical decisions: which layout primitive to use, which component boundaries,
where shared components are justified by real reuse.

## Unresolved questions

Anything unclear in the reference that needs confirmation before implementation.
